import React from 'react';
import { useParams } from "react-router-dom";
const MovieDetails = () => {
    return (
        <div>
            const {id}=useParams();
        </div>
    );
}

export default MovieDetails;
